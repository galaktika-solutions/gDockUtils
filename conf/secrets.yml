PG_CERTIFICATE_CACERT:
  postgres: /ca.crt:postgres:postgres:400
PG_CERTIFICATE_KEY:
  postgres: /server.key:postgres:postgres:400
PG_CERTIFICATE_CRT:
  postgres: /server.crt:postgres:postgres:400
DB_PASSWORD:
  django: DB_PASSWORD:django:django:400
